name: build
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: docker/build-push-action@v6
      - run: trivy image acme/orders:latest
      - run: npx semantic-release
services:
  postgres:
    image: postgres:16
