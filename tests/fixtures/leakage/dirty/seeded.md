# Seeded fixture

The line below carries the fixture token cbfixtureleak so the suite can
prove L-2 fires. The token is nonsense by construction: it must never
collide with a real wordlist entry.

Second occurrence, different line: CbFixtureLeak (case must not matter).

Boundary case that must NOT match: cbfixtureleakage is a longer word.
